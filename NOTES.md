# 踩坑日志

> 学习过程中遇到的概念难点、错误和心得。随学随记。

---

## 概念辨析

### 语句 vs 表达式（ex04）

- **语句**：不返回值，以 `;` 结尾（`let x = 5;` 是语句）
- **表达式**：返回值，无 `;`（`a + b` 是表达式）
- Rust 的 `if`、`loop`、代码块 `{}` 都是表达式，可以直接赋值
- 这就是为什么 Rust 不需要三元运算符：`let x = if cond { a } else { b };`

### println! 的 ln

`ln` = line，和 C# `Console.WriteLine()` 对应，`print!` = `Console.Write()`。

### `for x in (1..4)` 不是遍历数组

`(1..4)` 是 **Range 类型**，自己就能生成序列。和数组无关，不需要先定义一个数组再遍历。

`1..4` = [1, 4)（不包含 4），`1..=4` = [1, 4]（包含 4）。

### 判断偶数的正确写法（ex04）

❌ `n / 2 == 0` — 除法，判断商是否为 0（4/2=2，2≠0，误判为非偶数）
✅ `n % 2 == 0` — 取余，判断余数是否为 0

### 所有权：Move 在做什么

`let s2 = s1;` 不是复制数据，只是在栈上复制了 ptr + len + cap（24字节），堆上数据纹丝不动。然后 s1 被标记为失效。所以 Move 很快。

### String vs &str 的本质区别

- **String**：拥有者。栈结构体（ptr + len + cap）指向堆数据。离开作用域时释放堆内存。
- **&str**：借用者。栈结构体（ptr + len），没有 cap。离开作用域时什么也不释放。

`&String` 几乎不用，总是用 `&str` 更通用。

### 为什么 `&String` 能传给 `&str`？（Deref 强制转换）

```rust
fn bar(s: &str) {
    println!("{s}");
}

let s = String::from("hello");
bar(&s);  // &s 是 &String 类型，为什么可以传进去？
```

Rust 编译器会自动做 **Deref 强制转换**（Deref coercion）：

```rust
// 你写的：
bar(&s);

// 编译器自动做的（等价于）：
bar(&s[..]);          // 取 String 的全部切片
// 或者理解为自动调了 String.deref() → &str
```

所以 `&str` 参数能接收三种东西：

```rust
bar("字面量");            // &str → 直接匹配
bar(&String::from("x")); // &String → deref 自动转 &str
bar(&s[0..3]);           // 切片 → &str
```

一个签名兼容所有情况。Deref trait 会在第 16 课深入学习。

### 悬垂引用（Dangling Reference）

```rust
fn dangle() -> &String {
    let s = String::from("hello");
    &s  // ❌ s 在函数结束时被释放，返回的引用指向已释放内存
}
```

编译器会自动阻止这种代码。

---

## UTF-8 字节与切片

### 各字符占用字节数

| 字符类型 | 示例 | 字节数 |
|----------|------|--------|
| 英文/数字 | `a`, `1` | 1 |
| 常见中文 | `你`, `好` | 3 |
| 常见 emoji | `🐶`, `🦀` | 4 |
| 特殊 emoji | `👨‍👩‍👧` | 可能更多（组合序列） |

### 安全的切片方式

**永远别用字节索引去切混编字符串！**

```rust
let s = "Rust🦀中文";

// ❌ 危险：手动算字节容易切到字符中间 → panic
let bad = &s[0..5];  // 如果正好切在 emoji 中间就崩了

// ✅ 安全：用 .chars() 按字符操作
let good: String = s.chars().take(5).collect();  // "Rust🦀"
let good: String = s.chars().skip(2).take(3).collect();  // 第3~5个字符
```

### 中文字符串切片踩坑（ex07）

```rust
let s = "你好，世界！";

// ❌ s[0..3] → 只取了"你"（一个中文字 = 3 字节，所以 0..3 只是一个字）
// ✅ s[0..6] → 取了"你好"（两个字 = 6 字节）
```

结论：别自己算字节，用 `.chars()`。

---

## 借用规则速记

- 同一作用域内：
  - 可以有**多个**不可变引用 `&T`
  - 只能有**1 个**可变引用 `&mut T`
  - **不能**同时有可变和不可变引用
- 引用的作用域到**最后一次使用**为止，之后可以创建新引用
- 这些规则是为了在**编译时**消除数据竞争

---

## Debug vs Release

```bash
cargo run --bin ex03_types            # debug 模式（默认，溢出会 panic）
cargo run --bin ex03_types --release  # release 模式（溢出回绕，255+1=0）
```

---

## 底层概念：栈、寄存器、栈帧

> 学所有权时顺藤摸瓜了解到的。对写 Rust 不是必需的，但帮助理解"变量出作用域就没了"到底发生了什么。

### 寄存器是什么

CPU 内部自带的临时小格子，数量极少（x64 通用寄存器 16 个），速度最快（纳秒级）。
CPU 运算必须先读数据到寄存器，算完再写回内存——不能直接在内存里做加减。

| 寄存器 | 职责 |
|--------|------|
| RSP | 栈指针，指向当前栈顶，分配/回收靠它上下移动 |
| RBP | 栈基址，当前函数栈帧的"地基原点"，钉死不动 |
| RAX | 函数返回值默认放这里 |
| R8~R15 | 多出来的通用寄存器，常用于函数传参（R8、R9传第5、6个参数）|

### 栈的真相

栈不是固定格子，是**一块连续内存**（单线程通常 1~8MB），从高地址向低地址生长：

```
高地址 ──────────────────
  变量 a   [RBP - 8]
  变量 b   [RBP - 16]
  RSP →   （栈顶，往下挪 = 分配，往上挪 = 回收）
────────────────── 低地址
```

- **读取变量**：按地址直达，不用挪动上面的数据（不是翻盘子！）
- **分配变量**：RSP 往下移 `sub rsp, N`
- **回收变量**：RSP 往上移 `add rsp, N`（不擦数据，下次覆盖）
- **函数结束**：RSP 直接归位，栈帧内的变量全部"失效"

### 栈帧

每调用一个函数，就在大栈里切一小块叫做**栈帧**：

```
高地址 ──────────────
  栈帧 A（调用者）  RBP_A 固定
──────────────  ← 无缝紧贴，不留空隙
  栈帧 B（被调用）  RBP_B 固定
  RSP → 还能继续往下延伸
────────────── 低地址
```

- RBP = 当前函数栈帧的"栈底原点"，函数内钉死不动
- 栈帧之间紧挨着，帧内靠 RSP 自由伸展
- 新函数进来打新 RBP 标记，旧 RBP 保存起来，返回时恢复

### 越界踩踏

如果栈帧 B 无限递归/超大局部变量，RSP 一路往下冲：
1. 先踩坏 A 的局部变量 → 数值莫名改变
2. 再覆盖 A 的返回地址 → 程序跳飞、段错误闪退
3. 撞到线程栈物理边界 → Stack Overflow，系统杀进程

---

## 模块系统（ex10）

### mod 的文件查找规则

`mod xxx;` 只在**当前文件所在目录**和子目录里找：

```
src/bin/ex10.rs 中写 mod math;   →  搜 src/bin/math.rs 或 src/bin/math/mod.rs
```

不会向上翻目录，也不会去兄弟目录。

### 文件放在不同目录怎么办：#[path]

```rust
#[path = "../math_utils.rs"]   // 手动指定文件路径（相对于当前文件）
mod math_utils;                // 模块名照常写
```

### 文件里的代码不需要再包 mod { }

```rust
// math_utils.rs 里直接写代码就行：
pub fn add(a: i32, b: i32) -> i32 { a + b }

// ❌ 不要再包一层 mod math_utils { ... }，否则调用路径变成 math_utils::math_utils::add()
```

### super:: 是模块层级，不是目录层级

- `super::` = 往上一层**模块**（不是往上一层目录）
- 模块树的根（crate root）再往上没东西了，`super::` 在那里没用
- 想在模块中使用上层的路径：`crate::xxx`（从 crate 根开始）

### 项目的模块结构

- `src/main.rs` = 二进制 crate 根（有的话）
- `src/lib.rs` = 库 crate 根（有的话，所有 bin 都能用 `项目名::` 引用）
- `src/bin/*.rs` = 各自独立的 crate 根（没有 lib.rs 时它们是独立的）
- 本项目没有 `lib.rs`，所以每个 bin 文件都是独立的 crate，之间不能互相引用

### IDE 对文件位置的判断

`src/bin/` 下的所有 `.rs` 文件会被 Cargo 当成可执行程序，要求有 `main()`。模块文件应该放在 `src/` 下，如果放 `bin/` 下 IDE 会报"需要 main"的错。

---

## Vec 和 HashMap（ex12/ex13）

### Vec 常用操作速查

| 操作 | 方法 | 返回 |
|------|------|------|
| 追加 | `.push(x)` | — |
| 末尾弹出 | `.pop()` | `Option<T>` |
| 索引删除 | `.remove(i)` | `T`（越界 panic）|
| 安全访问 | `.get(i)` | `Option<&T>` |
| 不安全访问 | `v[i]` | `T`（越界 panic） |
| 可变访问 | `.get_mut(i)` | `Option<&mut T>` |

### HashMap 常用操作速查

| 操作 | 方法 | 说明 |
|------|------|------|
| 插入/覆盖 | `.insert(k, v)` | 旧值被覆盖 |
| 安全访问 | `.get(&k)` | 返回 `Option<&V>` |
| 只在不存在时插入 | `.entry(k).or_insert(v)` | 返回 `&mut V`，可累加 |

entry API 的词频统计是最常用的 HashMap 模式。

### 时间复杂度

描述的是**数据变多时操作会慢多少**，不是具体秒数：

| 复杂度 | 含义 | Vec 示例 |
|--------|------|----------|
| O(1) | 不管数据多大都一样快 | push/pop/get |
| O(n) | 数据大一倍，时间大一倍 | insert(0)/remove(0) |

---

## 所有权深入（ex14/ex15 后的补充）

### 普通变量 vs 常量/字面量的生命周期

| 类型 | 存在位置 | 生命周期 | 引用安全性 |
|------|----------|----------|-----------|
| 普通变量 | 栈/堆 | 代码块/函数内 | 必须遵守生命周期规则 |
| 常量/字面量 | 只读数据段 | 整个程序运行期（`'static`） | 永远安全 |

```rust
// 普通变量：在栈上，函数结束即销毁
fn main() {
    let x = 10;
    let r = &x;
} // x 被回收

// 字面量：在只读数据段，程序启动就在，退出才回收
let r = &10; // &10 是 'static 引用，永远有效
let s = "hello"; // 等价于 &'static str，同样是静态常量
```

### 返回值和引用的本质区别（最难理解的点）

**返回值 = 所有权转移，不会被销毁**
**返回引用 = 没所有权，原变量销毁，引用悬空**

```rust
fn create_number() -> i32 {
    let x = 100;
    x  // 返回时：值被搬走（所有权转移），函数结束时 x 是空壳
}

fn bad() -> &i32 {
    let x = 100;
    &x // ❌ 错误：引用带不走所有权，函数结束 x 销毁，引用变野指针
}
```

**编译器逻辑：**
1. 先看变量要不要被返回
2. 如果要返回原值 → 先把所有权转移给调用者 → 再销毁栈里的局部变量
3. 如果想返回引用 → 引用没有所有权 → 函数结束原变量销毁 → 引用指向空栈 → 编译器直接禁止

**极简口诀：** 值可以搬走，引用带不走；搬走再销毁，引用必悬空。

### 函数返回的比喻

函数像一个家，变量 x 像家具：
- `return x` = 把家具搬出去 → 房子拆了（函数结束）→ 家具完好无损
- `return &x` = 只给别人钥匙 → 房子拆了 → 钥匙变废钥匙

### clone() vs &（引用）

| 写法 | 本质 | 有没有复制数据 | 所有权 | 能否独立修改 |
|------|------|---------------|--------|------------|
| `book1.clone()` | 深拷贝/复制 | ✅ 有 | book2 是新主人 | ✅ 可以 |
| `&book1` | 引用/借用 | ❌ 完全没有 | 只有 book1 拥有 | ❌ 不行 |

```rust
let book2 = book1.clone();  // 复制出一份全新的独立副本，内容一样
let book2 = &book1;         // 只是借个引用，没有复制任何东西
```

clone 复制的是**内容**（不是地址），所以内容相同的数据用 `==` 比较会返回 true。

### Rust 的 == 和 C# 的 == 不一样

| 语言 | == 默认行为 |
|------|-----------|
| C# | 值类型比内容，引用类型比**内存地址**（是不是同一个对象） |
| Rust | 只要实现了 `PartialEq`，**永远比内容**，不管是不是同一个实例 |

```rust
// Rust：两个独立实例，但内容一样 → true
let b1 = Book { title: "Rust 入门".into(), pages: 300 };
let b2 = Book { title: "Rust 入门".into(), pages: 300 };
println!("{}", b1 == b2); // true（比内容）
```

```csharp
// C#：引用类型默认比地址，不同实例 → false
Book b1 = new Book("Rust入门", 300);
Book b2 = new Book("Rust入门", 300);
Console.WriteLine(b1 == b2); // False（比地址）
```

---

## 杂项

- `print!` 输出后不换行，`println!` 输出后换行
- Cargo.toml 中 `[[bin]]` 每行声明一个可执行文件，`cargo run --bin xxx` 运行
- 函数指定了返回类型但空函数体会编译失败，用 `todo!()` 占位可先让代码编译通过
